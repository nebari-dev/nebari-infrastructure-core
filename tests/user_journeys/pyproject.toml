[project]
name = "nebari_journeys"
version = "0.1.0"
description = "Reusable actions for Nebari user journey tests"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nebari_journeys"]
