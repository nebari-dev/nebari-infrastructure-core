[pytest]
markers =
    ui: journey drives a browser via Playwright
    slow: journey takes over 60 seconds
    tls: journey's SUBJECT is certificate validity or chain trust itself,
        as opposed to a journey that merely happens to travel over TLS;
        skipped on a cluster whose gateway anchor is a self-signed leaf
        (see nebari_journeys.trust.is_self_signed_leaf)
addopts = --strict-markers
