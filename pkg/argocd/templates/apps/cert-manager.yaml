apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: nebari-foundational
    app.kubernetes.io/managed-by: nebari-infrastructure-core
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: foundational

  source:
    chart: cert-manager
    repoURL: https://charts.jetstack.io
    targetRevision: v1.17.2
    helm:
      releaseName: cert-manager
      values: |
        installCRDs: true
        config:
          enableGatewayAPI: true
        # Serialize ACME HTTP-01 challenges to avoid a race when two Certificates
        # share a SAN (e.g. nebari-gateway-cert and nebari-landing-nebari-system-cert
        # both list <baseDomain>). Concurrent challenges for the same hostname
        # collide on Let's Encrypt's authorization record and one fails with
        # "No such authorization", leaving the cert stuck on failedIssuanceAttempts.
        # See nebari-dev/nebari-infrastructure-core#267.
        maxConcurrentChallenges: 1
        prometheus:
          enabled: true
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        webhook:
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
        cainjector:
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi

  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
