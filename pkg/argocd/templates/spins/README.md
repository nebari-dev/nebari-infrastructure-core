# Spins

User-owned directory watched recursively by the `spins` ArgoCD Application (`project: nebari-apps`). Commit spin manifests here and ArgoCD applies them. NIC seeds this README so the path exists on a freshly deployed platform.
