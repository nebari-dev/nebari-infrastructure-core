args:
  - start
replicas: 1
http:
  relativePath: "{{ .KeycloakBasePath }}/"
extraEnv: |
  - name: KEYCLOAK_ADMIN
    value: admin
  - name: KEYCLOAK_ADMIN_PASSWORD
    valueFrom:
      secretKeyRef:
        name: {{ .KeycloakAdminSecretName }}
        key: admin-password
  - name: KC_DB
    value: postgres
  - name: KC_DB_URL_HOST
    value: keycloak-db-rw.{{ .KeycloakNamespace }}.svc.cluster.local
  - name: KC_DB_URL_DATABASE
    value: keycloak
  # Both credentials come from the CNPG-generated keycloak-db-app
  # Secret so they cannot drift from initdb.owner in
  # manifests/keycloak/keycloak-db-cluster.yaml.
  - name: KC_DB_USERNAME
    valueFrom:
      secretKeyRef:
        name: keycloak-db-app
        key: username
  - name: KC_DB_PASSWORD
    valueFrom:
      secretKeyRef:
        name: keycloak-db-app
        key: password
  - name: KC_HOSTNAME
    value: "https://keycloak.{{ .Domain }}{{ .KeycloakBasePath }}"
  - name: KC_FEATURES
    value: "token-exchange,admin-fine-grained-authz:v1"
{{- if .TrustManagerEnabled }}
  # Keycloak 26 (Quarkus) ignores X509_CA_BUNDLE; the org CA is loaded
  # via the native truststore. KC_TRUSTSTORE_PATHS points at the
  # trust-manager-projected bundle mounted below and feeds every
  # outbound TLS call (IdP federation, token introspection).
  - name: KC_TRUSTSTORE_PATHS
    value: /etc/nebari/truststore
{{- end }}
proxy:
  mode: xforwarded
livenessProbe: |
  httpGet:
    path: {{ .KeycloakBasePath }}/health/live
    port: 9000
  initialDelaySeconds: 60
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3
readinessProbe: |
  httpGet:
    path: {{ .KeycloakBasePath }}/health/ready
    port: 9000
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3
startupProbe: |
  httpGet:
    path: {{ .KeycloakBasePath }}/health
    port: 9000
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 30
service:
  type: ClusterIP
  httpPort: 8080
ingress:
  enabled: false
postgresql:
  enabled: false
# CPU deliberately uncapped: logins are bursty and throttling hurts
# exactly when users pile in. The memory limit sizes the JVM heap
# (MaxRAMPercentage=70), so it must stay set. See #456/#457.
resources:
  requests:
    cpu: 250m
    memory: 1Gi
  limits:
    memory: 2Gi
{{- if .TrustManagerEnabled }}
extraVolumes: |
  - name: nebari-trust-bundle
    configMap:
      name: nebari-trust-bundle
      items:
        - key: ca-certificates.crt
          path: ca-certificates.crt
extraVolumeMounts: |
  - name: nebari-trust-bundle
    mountPath: /etc/nebari/truststore
    readOnly: true
{{- end }}
